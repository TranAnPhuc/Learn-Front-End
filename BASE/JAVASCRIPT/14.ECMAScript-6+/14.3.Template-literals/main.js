// Template literals
// 1. Chèn biến/biểu thức (Interpolation)
const course = "Javascript";
const description = `Khóa học  ${course} được dùng bằng \${}`;

console.log(description);

// 2. Chuỗi nhiều dòng (Multiline strings)
const lines = `Dòng 1
Dòng 2
Dòng 3`;

console.log(lines);
